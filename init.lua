local cmd = vim.cmd   --to call vim commands
local fn = vim.fn     --to call vim functions
local g = vim.g       --to access vim global variables

require "settings"

require "plugins"

require "lsp"
